<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reptedi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_application_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_application_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RepteDI">
        <activity
            android:name=".ChairsActivity"
            android:exported="false" />
        <activity
            android:name=".DetailActivity"
            android:exported="false" />
        <activity
            android:name=".PerformanceActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>